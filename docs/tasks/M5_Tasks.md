# タスクリスト

- M4 のチェックリストは `docs/tasks/M4_PLAN.md` を参照する。
- M5 の目的・スコープ・検証のたたき台は `docs/milestone/M5_PLAN.md` を参照する。

## M5: 文章類の整理（ドキュメント整備）

### M5-001: README の現行化
- [✅] `README.md` を最新のコード・依存関係・起動方法に合わせて更新する。  
      **確認観点**: `pyproject.toml` の依存、`main.py` のタブ構成、開発用と配布用（Nuitka 等）の説明が矛盾していないこと。  
      → 機能一覧、要件、実行手順、ドキュメントへのリンクを整理する。

### M5-002: アーキテクチャ文書の新規作成
- [✅] `docs/architecture.md` を新規作成し、Mermaid 等で全体構造を可視化する。  
      **確認観点**: 起動から各タブ生成までの流れ、主要パッケージ（`views` / `controllers` / `configurations`）、イベント・テーマ連携が追えること。  
      → クラス／モジュールの責務と代表的な呼び出し関係を図示する（全関数の列挙は不要）。

### M5-003: 説明タブの更新（任意・フィードバック次第）
- [-] ユーザーフィードバックに基づき、`views/description.py` の説明本文を更新する。  
      **確認観点**: 変更理由が `docs/milestone/M5_PLAN.md` またはタスク履歴から分かること。  
      → README / `architecture.md` と用語・手順が大きく食い違わないようにする。
